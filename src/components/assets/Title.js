import React from "react";

export const Title = ({title}) => {
    return (
        <div className={'title'}>
                <span>{title}
                    <h3>{title}</h3>
                </span>
        </div>
    )
};